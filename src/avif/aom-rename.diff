diff -pruN aom-3.0.0.o/build/cmake/generate_exports.cmake aom-3.0.0/build/cmake/generate_exports.cmake
--- aom-3.0.0.o/build/cmake/generate_exports.cmake	2021-04-15 22:13:55.763526000 +0300
+++ aom-3.0.0/build/cmake/generate_exports.cmake	2021-04-15 22:14:47.773598900 +0300
@@ -24,7 +24,7 @@ include("${AOM_ROOT}/build/cmake/exports
 if("${AOM_TARGET_SYSTEM}" STREQUAL "Darwin")
   set(symbol_prefix "_")
 elseif("${AOM_TARGET_SYSTEM}" MATCHES "Windows\|MSYS" AND AOM_MSVC)
-  file(WRITE "${AOM_SYM_FILE}" "LIBRARY aom\n" "EXPORTS\n")
+  file(WRITE "${AOM_SYM_FILE}" "LIBRARY aom-sail\n" "EXPORTS\n")
 else()
   set(symbol_suffix ";")
 endif()
@@ -34,7 +34,7 @@ set(aom_sym_file "${AOM_SYM_FILE}")
 if("${AOM_TARGET_SYSTEM}" STREQUAL "Darwin")
   file(REMOVE "${aom_sym_file}")
 elseif("${AOM_TARGET_SYSTEM}" MATCHES "Windows\|MSYS" AND AOM_MSVC)
-  file(WRITE "${aom_sym_file}" "LIBRARY aom\n" "EXPORTS\n")
+  file(WRITE "${aom_sym_file}" "LIBRARY aom-sail\n" "EXPORTS\n")
 else()
   file(WRITE "${aom_sym_file}" "{\nglobal:\n")
 endif()
diff -pruN aom-3.0.0.o/CMakeLists.txt aom-3.0.0/CMakeLists.txt
--- aom-3.0.0.o/CMakeLists.txt	2021-04-15 20:05:52.695181200 +0300
+++ aom-3.0.0/CMakeLists.txt	2021-04-15 22:14:29.383573100 +0300
@@ -247,6 +247,7 @@ if(ENABLE_EXAMPLES)
 endif()
 
 add_library(aom ${AOM_SOURCES} $<TARGET_OBJECTS:aom_rtcd>)
+set_target_properties(aom PROPERTIES OUTPUT_NAME aom-sail)
 if(BUILD_SHARED_LIBS)
   add_library(aom_static STATIC ${AOM_SOURCES} $<TARGET_OBJECTS:aom_rtcd>)
   set_target_properties(aom_static PROPERTIES OUTPUT_NAME aom)
